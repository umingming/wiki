# LLM Wiki 운영 규칙

이 디렉토리는 LLM이 관리하는 개인 지식 위키입니다. 목표는 채팅에서 흩어지는 답변을 남기지 않고, 시간이 갈수록 더 강해지는 영구적이고 복리적인 지식 자산을 유지하는 것입니다.

## 핵심 원칙

1. `sources/`는 원본 보관소이며 불변이다. 원본 자료는 저장 후 수정하지 않는다.
2. `pages/`는 LLM이 유지하는 작업 결과물이다. 새 소스를 읽거나 질문에 답할 때 기존 페이지를 우선 갱신한다.
3. 가치 있는 답변은 채팅으로만 끝내지 않는다. 재사용 가치가 있으면 위키 페이지로 재편입한다.
4. 모든 핵심 주장, 비교, 해석은 출처를 역추적할 수 있어야 한다.
5. 링크, 색인, 엔티티, 개념, 종합 분석은 함께 성장해야 한다. 고립된 페이지를 만들지 않는다.

## 워크플로우

### Ingest (소스 추가)
1. `sources/`에 원본 자료 저장
2. `pages/summaries/`에 소스별 요약 페이지 생성
3. 관련 `pages/concepts/`, `pages/entities/`, `pages/syntheses/`를 생성 또는 업데이트
4. 새 소스가 기존 주장과 충돌하면 차이를 명시하고 관련 페이지를 함께 수정
5. `index.md` 업데이트 (카테고리별 링크 + 한 줄 요약 + 필요시 메타데이터)
6. `log.md`에 `[YYYY-MM-DD HH:MM] **INGEST**` 형식으로 기록
7. Git 커밋

### Query (질의)
1. 먼저 `index.md`를 읽고 관련 페이지를 찾은 뒤 필요한 페이지를 탐색
2. 위키 페이지를 근거로 답변하고, 새 연결점이나 비교가 생기면 기존 페이지를 갱신
3. 재사용 가치가 있는 답변은 `pages/syntheses/`에 새 페이지로 저장하거나 기존 synthesis를 확장
4. 단발성 답변이라도 중요한 엔티티, 개념, 모순, 열린 질문이 드러나면 관련 페이지를 업데이트
5. `log.md`에 `[YYYY-MM-DD HH:MM] **QUERY**` 형식으로 기록

### Lint (정합성 검사)
1. 프론트매터 유효성, 깨진 링크, index 동기화 확인
2. 고아 페이지, 누락된 엔티티/개념 페이지, 약한 크로스레퍼런스 탐지
3. 출처 없는 핵심 주장, 오래된 주장, 상충하는 서술 탐지
4. 중요한 질의 결과가 위키에 재편입되지 않았는지 점검
5. `log.md`에 `[YYYY-MM-DD HH:MM] **LINT**` 형식으로 기록

## 페이지 컨벤션
- 모든 페이지에 YAML 프론트매터 필수 (`title`, `created`, `updated`, `sources`, `tags`)
- 파일명: 소문자, 하이픈 구분 (`transformer-architecture.md`)
- 내부 링크: 상대 경로 사용
- 가능한 한 기존 페이지를 확장하고, 중복 페이지 생성을 피한다
- `pages/syntheses/`의 새 문서는 `pages/syntheses/_template.md`를 출발점으로 사용한다
- 상세 규칙은 `schema.md` 참조
